using System.Collections.Generic;
using System.Reflection.PortableExecutable;

public class Program
{
    // PMG TO DO: REFACTOR

    public static void Main(string[] args)
    {
        /*
            NEXT CHALLENGES

            
            1. Count Non-Zero Numbers

            * Return how many numbers are not zero.
            * Non-zero means value != 0.
            * Example: [0, 1, 0, 3] -> 2
            * Example: [1, 2, 3] -> 3
            * Return 0 for null or empty.

            2. Move Target To End

            * Return a new array where all target values are moved to the end.
            * Keep non-target numbers in their original order.
            * Example: [1, 5, 10, 5], target 5 -> [1, 10, 5, 5]
            * First add non-target values, then add target values.
            * Return [] for null or empty.

            3. Find Second Largest Number

            * Return the second largest number in the array.
            * Track the largest number and second largest number.
            * If a value is bigger than largest, old largest becomes second largest.
            * Example: [1, 5, 3, 9, 2] -> 5
            * Example: [-5, -2, -1, -10] -> -2
            * Return 0 for null or empty.
            * If there is only one number, return that number.

            4. Find Most Frequent Number

            * Return the number that appears the most.
            * If two numbers appear the same number of times, return the one that appears first.
            * Example: [1, 2, 2, 3] -> 2
            * Example: [5, 5, 1, 1, 5] -> 5
            * Example: [4, 4, 2, 2] -> 4
            * Use nested loops.
            * Outer loop picks a number.
            * Inner loop counts how many times that number appears.
            * Return 0 for null or empty.

            5. Rotate Array Right By One

            * Return a new array with the last number moved to the front.
            * Example: [1, 2, 3, 4] -> [4, 1, 2, 3]
            * Example: [10, 20, 30] -> [30, 10, 20]
            * Create a new array.
            * Put the last value at index 0.
            * Copy the rest of the original values one position to the right.
            * Return [] for null or empty.
            * If there is only one number, return that number in a new array.

            6. Optional Stretch: Rotate Array Left By One

            * Return a new array with the first number moved to the end.
            * Example: [1, 2, 3, 4] -> [2, 3, 4, 1]
            * Example: [10, 20, 30] -> [20, 30, 10]
            * Create a new array.
            * Copy everything after index 0 one position left.
            * Put the original first value at the end.
            * Return [] for null or empty.
            * If there is only one number, return that number in a new array.
        */

    }
}
