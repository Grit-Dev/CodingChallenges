using System.Collections.Generic;
using System.Reflection.PortableExecutable;

public class Program
{
    // PMG TO DO: REFACTOR

    public static void Main(string[] args)
    {
        /*
            NEXT CHALLENGES

            1. Count Even Numbers

            * Return how many even numbers are in an array.
            * Even means value % 2 == 0.
            * Example: [1, 2, 3, 4, 5] -> 2
            * Return 0 for null or empty.

            2. Replace Target With Value

            * Return a new array.
            * If a number equals the target, replace it with the replacement value.
            * Otherwise, keep the original number.
            * Example: [1, 5, 10, 5], target 5, replacement 99 -> [1, 99, 10, 99]
            * Return [] for null or empty.

            3. Split Positive And Negative Numbers

            * Return a new array with positive numbers first, then negative numbers.
            * Ignore zero.
            * Positive means value > 0.
            * Negative means value < 0.
            * Example: [1, -2, 3, -4, 0] -> [1, 3, -2, -4]
            * Return [] for null or empty.

            4. Count Matching Numbers

            * Return how many times the target number appears in the array.
            * Example: [1, 5, 10, 5], target 5 -> 2
            * Return 0 for null or empty.

            5. Remove Target From Array

            * Return a new array with all target values removed.
            * Example: [1, 5, 10, 5], target 5 -> [1, 10]
            * Use a List<int>.
            * Add numbers only if value != target.
            * Return [] for null or empty.

            6. Optional Stretch: Move Target To End

            * Return a new array where all target values are moved to the end.
            * Keep non-target numbers in their original order.
            * Example: [1, 5, 10, 5], target 5 -> [1, 10, 5, 5]
            * First add non-target values, then add target values.
            * Return [] for null or empty.
        */

    }
}
