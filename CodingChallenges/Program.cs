using System.Diagnostics.Metrics;
using System.Globalization;
using System.Numerics;
using System.Text;

public class Program
{
    public static void Main(string[] args)
    {
        /*
            NEXT CHALLENGES

            1. Warm-up: Count Digits In String

            * Return how many digits are in a string.
            * Example: "abc123" -> 3
            * Example: "Cyberpunk 2077" -> 4
            * Example: "hello" -> 0
            * Return 0 for null or empty.

            2. Revision: Are Anagrams

            * Return true if both strings contain the same characters with the same counts.
            * Example: "listen", "silent" -> true
            * Example: "aab", "aba" -> true
            * Example: "aab", "ab" -> false
            * Example: "", "" -> true
            * Return false if either string is null.
            * Make it case-insensitive.
            * Use the count-each-character approach.
            * No Sort, LINQ, Dictionary, or HashSet.

            3. Revision: Count Unique Characters Case-Insensitive

            * Return how many different characters appear in a string.
            * Case-insensitive.
            * Example: "hello" -> 4
            * Example: "Swiss" -> 3
            * Example: "aabbcc" -> 3
            * Example: "Mississippi" -> 4
            * Return 0 for null or empty.
            * You may use List<char>.Contains().

            4. New Challenge: Remove Punctuation Characters

            * Return a new string with punctuation removed.
            * Example: "Hello!" -> "Hello"
            * Example: "Wait... what?" -> "Wait what"
            * Example: "C# is great." -> "C is great"
            * Return "" for null or empty.
            * Use char.IsPunctuation(character).

            5. New Challenge: Keep Only Letters And Spaces

            * Return a new string containing only letters and spaces.
            * Example: "Hello, World! 123" -> "Hello World "
            * Example: "C# is great." -> "C is great"
            * Example: "123!!!" -> ""
            * Return "" for null or empty.
            * Use char.IsLetter(character) or char.IsWhiteSpace(character).

            6. Optional Stretch: Count Each Letter A-Z

            * Return a formatted string showing how many times each letter appears.
            * Example: "abcaba" -> "a:3 b:2 c:1"
            * Ignore spaces, numbers, and symbols.
            * Case-insensitive.
            * No Dictionary, LINQ, or HashSet.
            * This is harder. Save it for last.
        */
    }
}
